<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MercadoLibreClone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/yellow</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MercadoLibreClone" parent="Base.Theme.MercadoLibreClone" />

    <style name="buttonStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/bg_btn_global</item>
        <item name="android:height">45dp</item>
    </style>

    <style name="progressBarStyle">
        <item name="colorAccent">@color/blue</item> <!-- Whatever color you want-->
    </style>


</resources>